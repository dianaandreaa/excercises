"use strict";

let names =  "Peter";

let newName = names[0].toLowerCase()+ names.substring(1);

console.log(names.toUpperCase());
console.log(newName);
console.log(names.substring(0, 3).toLowerCase()+names.substring(3).toUpperCase());
